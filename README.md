A simple web-based timer application. Served statically, users can choose between an incremental or decremental timer.

This is intended to be used for live broadcasts, e.g. through OBS. The timer can be captured using a window capture, while cropping any of the controls out.
